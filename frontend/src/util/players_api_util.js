updatePlayerTiles = (id) => {
  //get the player in this position
};

updatePlayerTargetWord = (id) => {
  // get the player in this position
};

updatePlayerClueArray = (id) => {
  //  get the player in this position
};

updatePlayerPoints = (id) => {
  //  get the player in this position
};

updatePlayerSubmitted = (id) => {
  //  get the player in this position
};

fetchPlayers = () => {
  //  get the player in this position
};
//after join game
createPlayer = (user_info) => {
  // makes a player from a user
};
